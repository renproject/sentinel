# `🔥 burn-sentry`

Logs any burns that haven't received their funds correctly.

## Development

Install dependencies:

```sh
yarn install
```

Setup `.env` file:

```sh
INFURA_KEY="..."
DATABASE_URL="postgres://..."
```

Start `🔥 burn-sentry`:

```sh
yarn start
```
